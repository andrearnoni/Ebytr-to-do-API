const ERROR = { message: 'Ops, an error occurred with your request' };

const INVALID_ENTRY = { message: 'Invalid entries. Try again.' };

const TODO_NOT_FOUND = { message: 'todo not found' };

module.exports = {
  ERROR,
  INVALID_ENTRY,
  TODO_NOT_FOUND,
};